---
// Import the global.css file here so that it is included on
// all pages through the use of the <BaseHead /> component.
import "../styles/global.css";
import type { ImageMetadata } from "astro";
import { GITHUB_PAGES_PATH_PREFIX, isDev, SITE_TITLE } from "../consts";

interface Props {
    title: string;
    description: string;
    image?: ImageMetadata;
}

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

const { title, description } = Astro.props;
---

<!-- Global Metadata -->
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />

<link
    rel="apple-touch-icon"
    sizes="180x180"
    href={isDev ? "/public/apple-touch-icon.png" : "/apple-touch-icon.png"}
/>
<link
    rel="icon"
    type="image/png"
    sizes="32x32"
    href={isDev ? "/public/favicon-32x32.png" : "/favicon-32x32.png"}
/>
<link
    rel="icon"
    type="image/png"
    sizes="16x16"
    href={isDev ? "/public/favicon-16x16.png" : "/favicon-16x16.png"}
/>
<link rel="manifest" href="/site.webmanifest" />

<link rel="sitemap" href="/sitemap-index.xml" />
<link
    rel="alternate"
    type="application/rss+xml"
    title={SITE_TITLE}
    href={new URL("rss.xml", Astro.site)}
/>
<meta name="generator" content={Astro.generator} />

<!-- Font preloads -->
<link
    rel="preload"
    href={GITHUB_PAGES_PATH_PREFIX + "/fonts/atkinson-regular.woff"}
    as="font"
    type="font/woff"
    crossorigin
/>
<link
    rel="preload"
    href={GITHUB_PAGES_PATH_PREFIX + "/fonts/atkinson-bold.woff"}
    as="font"
    type="font/woff"
    crossorigin
/>

<!-- Canonical URL -->
<link rel="canonical" href={canonicalURL} />

<!-- Primary Meta Tags -->
<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />
<meta name="author" />
